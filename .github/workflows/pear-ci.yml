name: Pear CI

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pear-ci
  cancel-in-progress: false

jobs:
  build:
    name: Build TypeScript + Bare linux-x64
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Compile harness + plugins
        run: npm run build:ts

      - name: Lint TypeScript
        run: npm run lint

      - name: Test harness (SDD/TDD)
        run: npm test

      - name: Build linux-x64 binary
        run: npm run make:linux-x64

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: pearledger-linux-x64
          path: out/linux-x64/
          if-no-files-found: warn

  stage:
    name: Stage pear:// release
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 15
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Stage with pear-ci
        uses: holepunchto/actions/pear-ci@main
        with:
          primary-key: ${{ secrets.PEAR_PRIMARY_KEY }}
          namespace: pearledger
          target: .
          dry-run: false

      - name: Verify upgrade link
        run: |
          node -e "
            const pkg = require('./package.json');
            if (pkg.upgrade.includes('YOUR_KEY')) {
              console.log('::warning::Replace pear:// key after first pear-ci run (see Actions log)');
              process.exit(0);
            }
            console.log('upgrade link:', pkg.upgrade);
          "
