# Inbox de facturas PDF — colocar archivos de prueba aquí

Los PDFs en esta carpeta no se versionan en git (ver `.gitignore`).

Ejemplo:
```bash
pearledger ingest ./workspace/invoices/factura-ejemplo.pdf
```
