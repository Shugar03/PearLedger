/**
 * Worker gasless: Pimlico/Candide + WDK settlement.
 * Expone MCP server wdk-mcp para Pista 1 del track WDK.
 */

console.log('[worker:wdk] WDK gasless worker stub')

// TODO: spawn wdk-mcp para Claude Desktop/Code/OpenClaw
// npm install -g @tetherto/wdk-cli@1.0.0-beta.2
// wdk mcp setup --ai claude-desktop

module.exports = { ready: true }
