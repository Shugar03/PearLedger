/**
 * Worker principal — OCR + LLM vía QVAC SDK
 * @see workspace/plugins/plugin-invoice-ops/ocr.ts
 */
export const QVAC_WORKER = 'main'

console.log('[worker:main] QVAC worker ready (OCR + LLM + RAG)')
